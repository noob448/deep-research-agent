# 深度研究智能体 依赖
# 安装: pip install -r requirements.txt

# LangChain 生态
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-core>=0.3.0

# deepagents 框架（LangChain 官方）
deepagents

# 搜索（免费，无需 API key）
ddgs>=8.0.0

# HTTP 请求
httpx>=0.27.0

# 检索重排（本地开源 reranker，免费，首次下载模型约 300MB）
sentence-transformers>=3.0.0

# 报告生成（python-docx，不用 pandoc）
python-docx>=1.0.0

# 本地向量库（RAG 知识库）
chromadb>=1.0.0
